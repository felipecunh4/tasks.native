/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TasList from './src/views/TaskList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TasList);
